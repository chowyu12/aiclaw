database:
  driver: sqlite
  dsn: ~/.aiclaw/aiclaw.db
  max_open_conns: 25
  max_idle_conns: 10
log:
  level: info
# Desktop runtime settings are stored in local SQLite. This file only controls
# database and logging initialization.
